const grid=document.querySelector('#grid'),empty=document.querySelector('#empty'),count=document.querySelector('#count'),q=document.querySelector('#q'),quickCats=document.querySelector('#quickCats');let products=[];const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const fmt=n=>n?Number(n).toLocaleString('ko-KR')+'원':'가격 확인';function render(list){count.textContent=`${list.length}개 상품`;grid.innerHTML='';empty.classList.toggle('hidden',list.length>0);for(const p of list){const el=document.createElement('article');el.className='product';el.innerHTML=`<img class="thumb" src="${p.thumbnail_url||'https://placehold.co/600x600?text=DoPick'}"><div class="product-body"><span class="product-code">제품번호 ${String(p.product_no).padStart(3,'0')}</span><h3>${esc(p.name)}</h3><div class="price">${fmt(p.price)}</div><p class="tagline">${esc(p.description||p.tags||p.category||'')}</p><div class="product-actions">${p.youtube_url?`<a target="_blank" href="${p.youtube_url}">영상 보기</a>`:''}<a class="buy" target="_blank" data-id="${p.id}" href="${p.affiliate_url}">상품 보기</a></div></div>`;grid.appendChild(el)}}function filter(t){t=t.trim().toLowerCase();render(!t?products:products.filter(p=>[p.product_no,p.name,p.category,p.tags,p.description].join(' ').toLowerCase().includes(t)))}async function load(){products=await(await fetch('/api/products')).json();render(products);const cats=[...new Set(products.map(x=>x.category).filter(Boolean))];quickCats.innerHTML=cats.map(c=>`<button class="chip" data-c="${esc(c)}">${esc(c)}</button>`).join('');quickCats.querySelectorAll('button').forEach(b=>b.onclick=()=>{q.value=b.dataset.c;filter(b.dataset.c)})}document.querySelector('#searchBtn').onclick=()=>filter(q.value);q.addEventListener('keydown',e=>e.key==='Enter'&&filter(q.value));document.querySelector('#requestForm').addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target));const j=await(await fetch('/api/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)})).json();document.querySelector('#requestMsg').textContent=j.ok?'요청이 접수됐습니다. 확인 후 연락드릴게요.':'전송에 실패했습니다.';if(j.ok)e.target.reset()});window.setRequestType=t=>document.querySelector('[name="type"]').value=t;load();
