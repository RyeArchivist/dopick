const grid=document.querySelector('#grid'),empty=document.querySelector('#empty'),count=document.querySelector('#count'),q=document.querySelector('#q'),quickCats=document.querySelector('#quickCats');let products=[];const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const fmt=n=>n!=null&&Number(n)>0?Number(n).toLocaleString('ko-KR')+'원':'';function render(list){count.textContent=`${list.length}개 상품`;grid.innerHTML='';empty.classList.toggle('hidden',list.length>0);for(const p of list){const el=document.createElement('article');el.className='product';el.innerHTML=`<img class="thumb" src="${p.thumbnail_url||'https://placehold.co/300x300?text=DoPick'}" alt="${esc(p.name)}"><div class="product-body"><div class="product-meta"><span class="product-code">${String(p.product_no).padStart(3,'0')}</span>${p.category?`<span class="product-category">${esc(p.category)}</span>`:''}</div><h3>${esc(p.name)}</h3>${fmt(p.price)?`<div class="price">${fmt(p.price)}</div>`:''}<p class="tagline">${esc(p.tags||p.description||'')}</p></div><a class="compact-buy" target="_blank" data-id="${p.id}" href="${p.affiliate_url}" aria-label="${esc(p.name)} 상품 보기"><span>상품보기</span><b>›</b></a>`;grid.appendChild(el)}}function filter(t){t=t.trim().toLowerCase();render(!t?products:products.filter(p=>[p.product_no,p.name,p.category,p.tags,p.description].join(' ').toLowerCase().includes(t)))}async function load(){products=await(await fetch('/api/products')).json();render(products);const cats=[...new Set(products.map(x=>x.category).filter(Boolean))];quickCats.innerHTML=cats.map(c=>`<button class="chip" data-c="${esc(c)}">${esc(c)}</button>`).join('');quickCats.querySelectorAll('button').forEach(b=>b.onclick=()=>{q.value=b.dataset.c;filter(b.dataset.c)})}document.querySelector('#searchBtn').onclick=()=>filter(q.value);q.addEventListener('keydown',e=>e.key==='Enter'&&filter(q.value));document.querySelector('#requestForm').addEventListener('submit',async e=>{e.preventDefault();const f=Object.fromEntries(new FormData(e.target));const j=await(await fetch('/api/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)})).json();document.querySelector('#requestMsg').textContent=j.ok?'요청이 접수됐습니다. 확인 후 연락드릴게요.':'전송에 실패했습니다.';if(j.ok)e.target.reset()});window.setRequestType=t=>document.querySelector('[name="type"]').value=t;load();
